#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(){
    char word[10],temp[10];
    int n;
    scanf("%d",&n);
    gets(temp);
    gets(word);
    // gets(word);
    // printf("%s",word);
    puts(word);
}